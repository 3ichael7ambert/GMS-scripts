/// @function           halftone_set_scale(scale);
/// @param {real}		scale Scale of halftone pattern (in pixels)
/// @description        Update halftone scale parameter

function halftone_set_scale(scale = v_scale)
{
	v_scale = scale;
	
	return scale;
}